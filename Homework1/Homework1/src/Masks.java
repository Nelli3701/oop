public class Masks extends Hygiene {
    public Masks(String productName,double productPrice, int productAmount, String productUnit, int amount) {
        super(productName, productPrice, productAmount, productUnit, amount);
    }
    @Override
    public String toString() {
        return super.toString();
    }
}
